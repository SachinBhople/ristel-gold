import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['i.imgur.com', 'www.tanishq.co.in', 'staticimg.tanishq.co.in'],
  },
};

export default nextConfig;
